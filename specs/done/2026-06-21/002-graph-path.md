# graph.path

## Meta

- status: done
- source: user-prompt

## 用户原始描述

1. 在图谱层实现知识路径搜索能力，返回节点链路、边类型和解释信息，而不只是文本命中。
2. 将该能力接到 local-store / client runtime / dmx CLI / MCP contracts 的现有图谱入口。
3. 为典型知识路径和断路情况补测试，并运行相关验证。

## TODO

- [x] 1. 在图谱层实现知识路径搜索能力，返回节点链路、边类型和解释信息，而不只是文本命中。
- [x] 2. 将该能力接到 local-store / client runtime / dmx CLI / MCP contracts 的现有图谱入口。
- [x] 3. 为典型知识路径和断路情况补测试，并运行相关验证。

## 实际行为记录

- 记录来源：只能记录已阅读代码、已修改代码、测试结果或用户确认的事实。
- 分支条件：完成后补充实际存在的正常、失败、边界、权限和状态分支。
- 默认参数行为：完成后补充源码里的默认值、配置来源和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令、结果和关联文件。
- 禁止事项：不要把猜测、常识或“看起来合理”的行为写成事实。

## Done

- doneAt: 2026-06-21T09:38:24.701Z
- note: graph.path implementation completed and verified with focused builds/tests.

## 最终行为契约

1. 未提供已验证行为
  - 条件：未提供来自代码、测试或用户确认的条件
  - 结果：不可作为真实行为事实
  - 默认行为：未验证
  - 边界处理：未验证
  - 验证：未提供验证证据
  - 关联文件：未记录
